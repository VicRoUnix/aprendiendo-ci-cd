services:
    app:
        build: ./node-app-health
        logging:
            driver: "json-file"
            options:
                max-size: "10m"
                max-file: "3"
        ports:
          - "3000:3000"